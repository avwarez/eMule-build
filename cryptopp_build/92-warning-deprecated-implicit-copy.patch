diff --git a/asn.h b/asn.h
index 41ecff3c..b302c685 100644
--- a/asn.h
+++ b/asn.h
@@ -265,6 +265,10 @@ class CRYPTOPP_DLL OID
 {
 public:
 	virtual ~OID() {}
+	// C5267: copying was left implicit next to a user-provided destructor, which
+	// is deprecated. Spelled out as defaulted; behaviour is unchanged.
+	OID(const OID &) = default;
+	OID & operator=(const OID &) = default;
 
 	/// \brief Construct an OID
 	OID() {}
diff --git a/cryptlib.h b/cryptlib.h
index 02c19c7d..6fce9cef 100644
--- a/cryptlib.h
+++ b/cryptlib.h
@@ -183,6 +183,10 @@ public:
 	};
 
 	virtual ~Exception() throw() {}
+	// C5267: copying was left implicit next to a user-provided destructor, which
+	// is deprecated. Spelled out as defaulted; behaviour is unchanged.
+	Exception(const Exception &) = default;
+	Exception & operator=(const Exception &) = default;
 
 	/// \brief Construct a new Exception
 	explicit Exception(ErrorType errorType, const std::string &s) : m_errorType(errorType), m_what(s) {}
@@ -590,6 +594,11 @@ class CRYPTOPP_DLL CRYPTOPP_NO_VTABLE Clonable
 {
 public:
 	virtual ~Clonable() {}
+	// C5267: copying was left implicit next to a user-provided destructor, which
+	// is deprecated. Spelled out as defaulted; behaviour is unchanged.
+	Clonable() = default;
+	Clonable(const Clonable &) = default;
+	Clonable & operator=(const Clonable &) = default;
 
 	/// \brief Copies  this object
 	/// \return a copy of this object
@@ -604,6 +613,10 @@ class CRYPTOPP_DLL CRYPTOPP_NO_VTABLE Algorithm : public Clonable
 {
 public:
 	virtual ~Algorithm() {}
+	// C5267: copying was left implicit next to a user-provided destructor, which
+	// is deprecated. Spelled out as defaulted; behaviour is unchanged.
+	Algorithm(const Algorithm &) = default;
+	Algorithm & operator=(const Algorithm &) = default;
 
 	/// \brief Interface for all crypto algorithms
 	/// \param checkSelfTestStatus determines whether the object can proceed if the self
@@ -1118,6 +1131,11 @@ class CRYPTOPP_DLL CRYPTOPP_NO_VTABLE HashTransformation : public Algorithm
 {
 public:
 	virtual ~HashTransformation() {}
+	// C5267: copying was left implicit next to a user-provided destructor, which
+	// is deprecated. Spelled out as defaulted; behaviour is unchanged.
+	HashTransformation() = default;
+	HashTransformation(const HashTransformation &) = default;
+	HashTransformation & operator=(const HashTransformation &) = default;
 
 	/// \brief Provides a reference to this object
 	/// \return A reference to this object
diff --git a/iterhash.h b/iterhash.h
index 49d32585..4bca1d26 100644
--- a/iterhash.h
+++ b/iterhash.h
@@ -41,6 +41,10 @@ public:
 	typedef T HashWordType;
 
 	virtual ~IteratedHashBase() {}
+	// C5267: copying was left implicit next to a user-provided destructor, which
+	// is deprecated. Spelled out as defaulted; behaviour is unchanged.
+	IteratedHashBase(const IteratedHashBase &) = default;
+	IteratedHashBase & operator=(const IteratedHashBase &) = default;
 
 	/// \brief Construct an IteratedHashBase
 	IteratedHashBase() : m_countLo(0), m_countHi(0) {}
@@ -134,6 +138,11 @@ public:
 	CRYPTOPP_COMPILE_ASSERT((T_BlockSize & (T_BlockSize - 1)) == 0);	// blockSize is a power of 2
 
 	virtual ~IteratedHash() {}
+	// C5267: copying was left implicit next to a user-provided destructor, which
+	// is deprecated. Spelled out as defaulted; behaviour is unchanged.
+	IteratedHash() = default;
+	IteratedHash(const IteratedHash &) = default;
+	IteratedHash & operator=(const IteratedHash &) = default;
 
 	/// \brief Provides the block size of the hash
 	/// \return the block size of the hash, in bytes
@@ -184,6 +193,10 @@ public:
 	CRYPTOPP_CONSTANT(DIGESTSIZE = T_DigestSize ? T_DigestSize : T_StateSize);
 
 	virtual ~IteratedHashWithStaticTransform() {}
+	// C5267: copying was left implicit next to a user-provided destructor, which
+	// is deprecated. Spelled out as defaulted; behaviour is unchanged.
+	IteratedHashWithStaticTransform(const IteratedHashWithStaticTransform &) = default;
+	IteratedHashWithStaticTransform & operator=(const IteratedHashWithStaticTransform &) = default;
 
 	/// \brief Provides the digest size of the hash
 	/// \return the digest size of the hash, in bytes
diff --git a/modarith.h b/modarith.h
index a92aafe9..29ffef4e 100644
--- a/modarith.h
+++ b/modarith.h
@@ -296,6 +296,10 @@ class CRYPTOPP_DLL MontgomeryRepresentation : public ModularArithmetic
 {
 public:
 	virtual ~MontgomeryRepresentation() {}
+	// C5267: copying was left implicit next to a user-provided destructor, which
+	// is deprecated. Spelled out as defaulted; behaviour is unchanged.
+	MontgomeryRepresentation(const MontgomeryRepresentation &) = default;
+	MontgomeryRepresentation & operator=(const MontgomeryRepresentation &) = default;
 
 	/// \brief Construct a MontgomeryRepresentation
 	/// \param modulus congruence class modulus
