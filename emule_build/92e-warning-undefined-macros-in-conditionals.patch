diff --git a/srchybrid/CaptchaGenerator.cpp b/srchybrid/CaptchaGenerator.cpp
index 5f23ce2..66e9021 100644
--- a/srchybrid/CaptchaGenerator.cpp
+++ b/srchybrid/CaptchaGenerator.cpp
@@ -112,7 +112,7 @@ void CCaptchaGenerator::ReGenerateCaptcha(uint32 nLetterCount)
 	::DeleteDC(hdcMem);
 	::SelectObject(hdc, hBitmapOld);
 	::DeleteDC(hdc);
-#if TEST_FRAMEGRABBER //reusing macro from FrameGrabThread
+#ifdef TEST_FRAMEGRABBER //reusing macro from FrameGrabThread
 	CImage captcha;
 	captcha.Attach(m_hbmpCaptcha);
 	captcha.Save(_T("\\tmp\\CaptchaTest.bmp"), Gdiplus::ImageFormatBMP);
diff --git a/srchybrid/FrameGrabThread.cpp b/srchybrid/FrameGrabThread.cpp
index 8edc0b2..6223839 100644
--- a/srchybrid/FrameGrabThread.cpp
+++ b/srchybrid/FrameGrabThread.cpp
@@ -41,7 +41,7 @@
 #define MMNOMMSYSTEM	// mmsystem: General MMSYSTEM functions
 #include <dshow.h>
 
-#if TEST_FRAMEGRABBER
+#ifdef TEST_FRAMEGRABBER
 #include <atlimage.h>
 #endif
 
@@ -228,7 +228,7 @@ UINT CFrameGrabThread::GrabFrames()
 				::SelectObject(hdc, hBitmapOld);
 				// done
 				imgResults[nFramesGrabbed] = hFrame;
-#if TEST_FRAMEGRABBER //see also "case MP_OPEN" in CSharedFilesCtrl::OnContextMenu
+#ifdef TEST_FRAMEGRABBER //see also "case MP_OPEN" in CSharedFilesCtrl::OnContextMenu
 				CString TestName;
 				TestName.Format(_T("\\tmp\\testframe%i.bmp"), nFramesGrabbed);
 				CImage img;
diff --git a/srchybrid/GZipFile.h b/srchybrid/GZipFile.h
index ff9c7b5..cba3599 100644
--- a/srchybrid/GZipFile.h
+++ b/srchybrid/GZipFile.h
@@ -16,7 +16,7 @@
 //Foundation, Inc., 675 Mass Ave, Cambridge, MA 02139, USA.
 #pragma once
 
-#if ZLIB_VERNUM < 0x1260
+#if !defined(ZLIB_VERNUM) || ZLIB_VERNUM < 0x1260
 typedef void *gzFile;
 #endif
 
diff --git a/srchybrid/Parser.cpp b/srchybrid/Parser.cpp
index 92f4072..3ce3f1b 100644
--- a/srchybrid/Parser.cpp
+++ b/srchybrid/Parser.cpp
@@ -145,6 +145,10 @@ static char THIS_FILE[] = __FILE__;
 
 extern CStringArray g_astrParserErrors;
 
+// bison tests YYLTYPE_IS_TRIVIAL with #if but only defines it when locations are
+// in use; this grammar has none, so give it the value it was read as anyway.
+#define YYLTYPE_IS_TRIVIAL 0
+
 void ParsedSearchExpression(const CSearchExpr &expr);
 int yyerror(LPCTSTR errstr);
 int yyerrorf(LPCTSTR errstr, ...);
diff --git a/srchybrid/Parser.y b/srchybrid/Parser.y
index 56e1fcb..ee0002c 100644
--- a/srchybrid/Parser.y
+++ b/srchybrid/Parser.y
@@ -15,6 +15,10 @@ static char THIS_FILE[] = __FILE__;
 
 extern CStringArray g_astrParserErrors;
 
+// bison tests YYLTYPE_IS_TRIVIAL with #if but only defines it when locations are
+// in use; this grammar has none, so give it the value it was read as anyway.
+#define YYLTYPE_IS_TRIVIAL 0
+
 void ParsedSearchExpression(const CSearchExpr &expr);
 int yyerror(LPCTSTR errstr);
 int yyerrorf(LPCTSTR errstr, ...);
diff --git a/srchybrid/SharedFilesCtrl.cpp b/srchybrid/SharedFilesCtrl.cpp
index d080952..c6019eb 100644
--- a/srchybrid/SharedFilesCtrl.cpp
+++ b/srchybrid/SharedFilesCtrl.cpp
@@ -864,7 +864,7 @@ BOOL CSharedFilesCtrl::OnCommand(WPARAM wParam, LPARAM)
 #endif
 		// file operations
 		case MP_OPEN:
-#if TEST_FRAMEGRABBER //see also FrameGrabThread::GrabFrames
+#ifdef TEST_FRAMEGRABBER //see also FrameGrabThread::GrabFrames
 			if (file) {
 				CKnownFile *previewFile = theApp.sharedfiles->GetFileByID(file->GetFileHash());
 				if (previewFile != NULL)
