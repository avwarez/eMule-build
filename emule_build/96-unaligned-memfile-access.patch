diff --git a/srchybrid/SafeFile.cpp b/srchybrid/SafeFile.cpp
index c59d254..f9fef19 100644
--- a/srchybrid/SafeFile.cpp
+++ b/srchybrid/SafeFile.cpp
@@ -294,7 +294,8 @@ uint16 CSafeMemFile::ReadUInt16()
 {
 	if (m_nPosition + sizeof(uint16) > m_nFileSize)
 		AfxThrowFileException(CFileException::endOfFile, 0, GetFileName());
-	uint16 nResult = *(uint16*)&m_lpBuffer[m_nPosition];
+	uint16 nResult;
+	memcpy(&nResult, &m_lpBuffer[m_nPosition], sizeof(uint16));
 	m_nPosition += sizeof(uint16);
 	return nResult;
 }
@@ -303,7 +304,8 @@ uint32 CSafeMemFile::ReadUInt32()
 {
 	if (m_nPosition + sizeof(uint32) > m_nFileSize)
 		AfxThrowFileException(CFileException::endOfFile, 0, GetFileName());
-	uint32 nResult = *(uint32*)&m_lpBuffer[m_nPosition];
+	uint32 nResult;
+	memcpy(&nResult, &m_lpBuffer[m_nPosition], sizeof(uint32));
 	m_nPosition += sizeof(uint32);
 	return nResult;
 }
@@ -312,7 +314,8 @@ uint64 CSafeMemFile::ReadUInt64()
 {
 	if (m_nPosition + sizeof(uint64) > m_nFileSize)
 		AfxThrowFileException(CFileException::endOfFile, 0, GetFileName());
-	uint64 nResult = *(uint64*)&m_lpBuffer[m_nPosition];
+	uint64 nResult;
+	memcpy(&nResult, &m_lpBuffer[m_nPosition], sizeof(uint64));
 	m_nPosition += sizeof(uint64);
 	return nResult;
 }
@@ -343,7 +346,7 @@ void CSafeMemFile::WriteUInt16(uint16 nVal)
 {
 	if (m_nPosition + sizeof(uint16) > m_nBufferSize)
 		GrowFile(m_nPosition + sizeof(uint16));
-	*(uint16*)&m_lpBuffer[m_nPosition] = nVal;
+	memcpy(&m_lpBuffer[m_nPosition], &nVal, sizeof(uint16));
 	m_nPosition += sizeof(uint16);
 	if (m_nPosition > m_nFileSize)
 		m_nFileSize = m_nPosition;
@@ -353,7 +356,7 @@ void CSafeMemFile::WriteUInt32(uint32 nVal)
 {
 	if (m_nPosition + sizeof(uint32) > m_nBufferSize)
 		GrowFile(m_nPosition + sizeof(uint32));
-	*(uint32*)&m_lpBuffer[m_nPosition] = nVal;
+	memcpy(&m_lpBuffer[m_nPosition], &nVal, sizeof(uint32));
 	m_nPosition += sizeof(uint32);
 	if (m_nPosition > m_nFileSize)
 		m_nFileSize = m_nPosition;
@@ -363,7 +366,7 @@ void CSafeMemFile::WriteUInt64(uint64 nVal)
 {
 	if (m_nPosition + sizeof(uint64) > m_nBufferSize)
 		GrowFile(m_nPosition + sizeof(uint64));
-	*(uint64*)&m_lpBuffer[m_nPosition] = nVal;
+	memcpy(&m_lpBuffer[m_nPosition], &nVal, sizeof(uint64));
 	m_nPosition += sizeof(uint64);
 	if (m_nPosition > m_nFileSize)
 		m_nFileSize = m_nPosition;
