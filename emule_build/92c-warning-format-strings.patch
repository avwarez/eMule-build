diff --git a/srchybrid/EmuleDlg.cpp b/srchybrid/EmuleDlg.cpp
index 14b6f2c..b5c4fd2 100644
--- a/srchybrid/EmuleDlg.cpp
+++ b/srchybrid/EmuleDlg.cpp
@@ -1400,10 +1400,9 @@ LRESULT CemuleDlg::OnWMData(WPARAM, LPARAM lParam)
 					else
 						uid = IDS_DISCONNECTED;
 					_ftprintf(file, _T("%s\n"), (LPCTSTR)GetResString(uid));
-#pragma warning(push)
-#pragma warning(disable:4774)
+					// the format is the translated IDS_UPDOWNSMALL, which cannot be a literal
+#pragma warning(suppress: 4774)
 					_ftprintf(file, (LPCTSTR)GetResString(IDS_UPDOWNSMALL), (float)theApp.uploadqueue->GetDatarate() / 1024, (float)theApp.downloadqueue->GetDatarate() / 1024);
-#pragma warning(pop)
 					// next string (getTextList) is already prefixed with '\n'!
 					_ftprintf(file, _T("%s\n"), (LPCTSTR)transferwnd->GetDownloadList().getTextList());
 
diff --git a/srchybrid/Ini2.cpp b/srchybrid/Ini2.cpp
index 8fd11e8..19a7822 100644
--- a/srchybrid/Ini2.cpp
+++ b/srchybrid/Ini2.cpp
@@ -331,18 +331,15 @@ bool CIni::GetBool(LPCTSTR lpszEntry, bool bDefault, LPCTSTR lpszSection)
 	return _tstoi(GetString(lpszEntry, szDefault, lpszSection)) != 0;
 }
 
-#pragma warning(push)
-#pragma warning(disable:4774)
 CPoint CIni::GetPoint(LPCTSTR lpszEntry, const CPoint &ptDefault, LPCTSTR lpszSection)
 {
-	static LPCTSTR const pszFmt = _T("(%ld,%ld)");
 	CPoint ptReturn(ptDefault);
 
 	CString strDefault;
-	strDefault.Format(pszFmt, ptDefault.x, ptDefault.y);
+	strDefault.Format(_T("(%ld,%ld)"), ptDefault.x, ptDefault.y);
 
 	const CString &strPoint(GetString(lpszEntry, strDefault, lpszSection));
-	if (_stscanf(strPoint, pszFmt, &ptReturn.x, &ptReturn.y) != 2)
+	if (_stscanf(strPoint, _T("(%ld,%ld)"), &ptReturn.x, &ptReturn.y) != 2)
 		return ptDefault;
 
 	return ptReturn;
@@ -350,22 +347,20 @@ CPoint CIni::GetPoint(LPCTSTR lpszEntry, const CPoint &ptDefault, LPCTSTR lpszSe
 
 CRect CIni::GetRect(LPCTSTR lpszEntry, const CRect &rcDefault, LPCTSTR lpszSection)
 {
-	static LPCTSTR const pszFmt = _T("%ld,%ld,%ld,%ld");
 	CRect rcReturn(rcDefault);
 	//prepare default string
 	CString strDefault;
-	strDefault.Format(pszFmt, rcDefault.left, rcDefault.top, rcDefault.right, rcDefault.bottom);
+	strDefault.Format(_T("%ld,%ld,%ld,%ld"), rcDefault.left, rcDefault.top, rcDefault.right, rcDefault.bottom);
 	//read settings
 	const CString &strRect(GetString(lpszEntry, strDefault, lpszSection));
 	//try as the new Version first, then check the old version
-	if (_stscanf(strRect, pszFmt, &rcReturn.top, &rcReturn.left, &rcReturn.bottom, &rcReturn.right) != 4
+	if (_stscanf(strRect, _T("%ld,%ld,%ld,%ld"), &rcReturn.top, &rcReturn.left, &rcReturn.bottom, &rcReturn.right) != 4
 		&& _stscanf(strRect, _T("(%ld,%ld,%ld,%ld)"), &rcReturn.top, &rcReturn.left, &rcReturn.bottom, &rcReturn.right) != 4)
 	{
 		return rcDefault; //both versions failed, fall back to defaults
 	}
 	return rcReturn;
 }
-#pragma warning(pop)
 
 COLORREF CIni::GetColRef(LPCTSTR lpszEntry, COLORREF crDefault, LPCTSTR lpszSection)
 {
diff --git a/srchybrid/Preferences.cpp b/srchybrid/Preferences.cpp
index 1bf3a46..32ccf9b 100644
--- a/srchybrid/Preferences.cpp
+++ b/srchybrid/Preferences.cpp
@@ -671,24 +671,15 @@ bool CPreferences::IsTempFile(const CString &rstrDirectory, const CString &rstrN
 	CString strNameLower(rstrName);
 	strNameLower.MakeLower();
 	strNameLower += _T("|"); // append an EOS character which we can query for
-	static LPCTSTR const _apszNotSharedExts[] = {
-		_T("%u.part") _T("%c"),
-		_T("%u.part.met") _T("%c"),
-		_T("%u.part.met") PARTMET_BAK_EXT _T("%c"),
-		_T("%u.part.met") PARTMET_TMP_EXT _T("%c")
-	};
-	for (unsigned i = 0; i < _countof(_apszNotSharedExts); ++i) {
-		UINT uNum;
-		TCHAR iChar;
-		// "misuse" the 'scanf' function for a very simple pattern scanning.
-#pragma warning(push)
-#pragma warning(disable:4774)
-		if (_stscanf(strNameLower, _apszNotSharedExts[i], &uNum, &iChar) == 2 && iChar == _T('|'))
-			return true;
-#pragma warning(pop)
-	}
-
-	return false;
+	UINT uNum;
+	TCHAR iChar;
+	// "misuse" the 'scanf' function for a very simple pattern scanning.
+	// Each pattern is spelled out as a literal so that the compiler checks it
+	// against the arguments.
+	return (_stscanf(strNameLower, _T("%u.part%c"), &uNum, &iChar) == 2 && iChar == _T('|'))
+		|| (_stscanf(strNameLower, _T("%u.part.met%c"), &uNum, &iChar) == 2 && iChar == _T('|'))
+		|| (_stscanf(strNameLower, _T("%u.part.met") PARTMET_BAK_EXT _T("%c"), &uNum, &iChar) == 2 && iChar == _T('|'))
+		|| (_stscanf(strNameLower, _T("%u.part.met") PARTMET_TMP_EXT _T("%c"), &uNum, &iChar) == 2 && iChar == _T('|'));
 }
 
 uint64 CPreferences::GetMaxDownloadInBytesPerSec(bool bDynamic)
diff --git a/srchybrid/TreeOptionsCtrlEx.cpp b/srchybrid/TreeOptionsCtrlEx.cpp
index 611a90a..0af141f 100644
--- a/srchybrid/TreeOptionsCtrlEx.cpp
+++ b/srchybrid/TreeOptionsCtrlEx.cpp
@@ -411,13 +411,13 @@ void EditTextWithFormat(CDataExchange *pDX, int nIDC, HTREEITEM hItem, LPCTSTR l
 		void *pResult = va_arg(pData, void*);
 		// the following works for %d, %u, %ld, %lu
 		CString sText(pCtrlTreeOptions->GetEditText(hItem));
-#pragma warning(push)
-#pragma warning(disable:4774)
+		// lpszFormat is the caller's, as in MFC's DDX_TextWithFormat(); every caller
+		// below passes a literal matching the type it hands over
+#pragma warning(suppress: 4774)
 		if (_stscanf(sText, lpszFormat, pResult) != 1) {
 			AfxMessageBox(nIDPrompt);
 			pDX->Fail();	// throws exception
 		}
-#pragma warning(pop)
 	} else {
 		TCHAR szT[64];
 		_vsntprintf(szT, _countof(szT), lpszFormat, pData);
