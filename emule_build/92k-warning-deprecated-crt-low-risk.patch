diff --git a/srchybrid/DownloadQueue.cpp b/srchybrid/DownloadQueue.cpp
index 9f7c445..befd784 100644
--- a/srchybrid/DownloadQueue.cpp
+++ b/srchybrid/DownloadQueue.cpp
@@ -1650,7 +1650,7 @@ void CDownloadQueue::ExportPartMetFilesOverview() const
 				const CString &strPartFilePath(pPartFile->GetFilePath());
 				TCHAR szNam[_MAX_FNAME];
 				TCHAR szExt[_MAX_EXT];
-				_tsplitpath(strPartFilePath, NULL, NULL, szNam, szExt);
+				_tsplitpath_s(strPartFilePath, NULL, 0, NULL, 0, szNam, _countof(szNam), szExt, _countof(szExt));
 				if (thePrefs.GetTempDirCount() == 1)
 					file.printf(_T("%s%s\t%s\r\n"), szNam, szExt, (LPCTSTR)pPartFile->GetED2kLink());
 				else
diff --git a/srchybrid/Emule.cpp b/srchybrid/Emule.cpp
index 492d981..beb0208 100644
--- a/srchybrid/Emule.cpp
+++ b/srchybrid/Emule.cpp
@@ -623,8 +623,10 @@ static int CrtDebugReportCB(int reportType, char *message, int *returnValue) noe
 	FILE *fp = _tfsopen(s_szCrtDebugReportFilePath, _T("a"), _SH_DENYWR);
 	if (fp) {
 		time_t tNow = time(NULL);
+		struct tm tmNow;
+		localtime_s(&tmNow, &tNow);
 		TCHAR szTime[40];
-		_tcsftime(szTime, _countof(szTime), _T("%H:%M:%S"), localtime(&tNow));
+		_tcsftime(szTime, _countof(szTime), _T("%H:%M:%S"), &tmNow);
 		_ftprintf(fp, _T("%s  %i  %hs"), szTime, reportType, message);
 		fclose(fp);
 	}
diff --git a/srchybrid/GZipFile.cpp b/srchybrid/GZipFile.cpp
index 3e512de..fd264ea 100644
--- a/srchybrid/GZipFile.cpp
+++ b/srchybrid/GZipFile.cpp
@@ -89,7 +89,8 @@ CString CGZIPFile::GetUncompressedFileName() const
 
 bool CGZIPFile::Extract(LPCTSTR pszFilePath)
 {
-	int fdOut = _tsopen(pszFilePath, _O_CREAT | _O_TRUNC | _O_WRONLY | _O_BINARY, _SH_DENYWR, _S_IREAD | _S_IWRITE);
+	int fdOut;
+	_tsopen_s(&fdOut, pszFilePath, _O_CREAT | _O_TRUNC | _O_WRONLY | _O_BINARY, _SH_DENYWR, _S_IREAD | _S_IWRITE);
 	if (fdOut == -1)
 		return false;
 
diff --git a/srchybrid/HTRichEditCtrl.cpp b/srchybrid/HTRichEditCtrl.cpp
index e3b9107..1ff83c4 100644
--- a/srchybrid/HTRichEditCtrl.cpp
+++ b/srchybrid/HTRichEditCtrl.cpp
@@ -566,14 +566,14 @@ bool CHTRichEditCtrl::SaveLog(LPCTSTR pszDefName)
 			fwrite(strText, sizeof(TCHAR), strText.GetLength(), fp);
 			if (ferror(fp)) {
 				CString strError;
-				strError.Format(_T("Failed to write log file \"%s\" - %s"), (LPCTSTR)dlg.GetPathName(), _tcserror(errno));
+				strError.Format(_T("Failed to write log file \"%s\" - %s"), (LPCTSTR)dlg.GetPathName(), (LPCTSTR)GetErrnoMessage(errno));
 				AfxMessageBox(strError, MB_ICONERROR);
 			} else
 				bResult = true;
 			fclose(fp);
 		} else {
 			CString strError;
-			strError.Format(_T("Failed to create log file \"%s\" - %s"), (LPCTSTR)dlg.GetPathName(), _tcserror(errno));
+			strError.Format(_T("Failed to create log file \"%s\" - %s"), (LPCTSTR)dlg.GetPathName(), (LPCTSTR)GetErrnoMessage(errno));
 			AfxMessageBox(strError, MB_ICONERROR);
 		}
 	}
diff --git a/srchybrid/I18n.cpp b/srchybrid/I18n.cpp
index f72a794..c520848 100644
--- a/srchybrid/I18n.cpp
+++ b/srchybrid/I18n.cpp
@@ -150,7 +150,7 @@ static void InitLanguages(const CString &rstrLangDir1, const CString &rstrLangDi
 		if (ff.IsDirectory())
 			continue;
 		TCHAR szLangDLLFileName[_MAX_FNAME];
-		_tsplitpath(ff.GetFileName(), NULL, NULL, szLangDLLFileName, NULL);
+		_tsplitpath_s(ff.GetFileName(), NULL, 0, NULL, 0, szLangDLLFileName, _countof(szLangDLLFileName), NULL, 0);
 
 		for (SLanguage *pLang = s_aLanguages; pLang->lid; ++pLang)
 			if (_tcsicmp(pLang->pszISOLocale, szLangDLLFileName) == 0) {
diff --git a/srchybrid/IPFilter.cpp b/srchybrid/IPFilter.cpp
index cc0a0ea..e29782c 100644
--- a/srchybrid/IPFilter.cpp
+++ b/srchybrid/IPFilter.cpp
@@ -93,7 +93,7 @@ INT_PTR CIPFilter::AddFromFile(LPCTSTR pszFilePath, bool bShowResponse)
 
 			TCHAR szNam[_MAX_FNAME];
 			TCHAR szExt[_MAX_EXT];
-			_tsplitpath(pszFilePath, NULL, NULL, szNam, szExt);
+			_tsplitpath_s(pszFilePath, NULL, 0, NULL, 0, szNam, _countof(szNam), szExt, _countof(szExt));
 			if (_tcsicmp(szExt, _T(".p2p")) == 0 || (_tcsicmp(szNam, _T("guarding.p2p")) == 0 && _tcsicmp(szExt, _T(".txt")) == 0))
 				eFileType = PeerGuardian;
 			else if (_tcsicmp(szExt, _T(".prefix")) == 0)
@@ -207,7 +207,7 @@ INT_PTR CIPFilter::AddFromFile(LPCTSTR pszFilePath, bool bShowResponse)
 				}
 			}
 		} catch (...) {
-			AddDebugLogLine(false, _T("Exception when loading IP filters - %s"), _tcserror(errno));
+			AddDebugLogLine(false, _T("Exception when loading IP filters - %s"), (LPCTSTR)GetErrnoMessage(errno));
 			fclose(readFile);
 			throw;
 		}
@@ -280,7 +280,7 @@ void CIPFilter::SaveToDefaultFile()
 
 			if (fprintf(fp, "%-15s - %-15s , %3u , %s\n", szStart, szEnd, flt->level, (LPCSTR)flt->desc) == 0 || ferror(fp)) {
 				CString strError;
-				strError.Format(_T("Failed to save IP filter to file \"%s\" - %s"), (LPCTSTR)strFilePath, _tcserror(errno));
+				strError.Format(_T("Failed to save IP filter to file \"%s\" - %s"), (LPCTSTR)strFilePath, (LPCTSTR)GetErrnoMessage(errno));
 				fclose(fp);
 				throw strError;
 			}
@@ -289,7 +289,7 @@ void CIPFilter::SaveToDefaultFile()
 		m_bModified = false;
 	} else {
 		CString strError;
-		strError.Format(_T("Failed to save IP filter to file \"%s\" - %s"), (LPCTSTR)strFilePath, _tcserror(errno));
+		strError.Format(_T("Failed to save IP filter to file \"%s\" - %s"), (LPCTSTR)strFilePath, (LPCTSTR)GetErrnoMessage(errno));
 		throw strError;
 	}
 }
diff --git a/srchybrid/IPFilterDlg.cpp b/srchybrid/IPFilterDlg.cpp
index 3748a59..ec4f03f 100644
--- a/srchybrid/IPFilterDlg.cpp
+++ b/srchybrid/IPFilterDlg.cpp
@@ -256,13 +256,13 @@ void CIPFilterDlg::OnLvnGetDispInfoIPFilter(LPNMHDR pNMHDR, LRESULT *pResult)
 			break;
 		case IPFILTER_COL_LEVEL:
 			if (rItem.cchTextMax > 0) {
-				_tcsncpy(rItem.pszText, _itot(m_ppIPFilterItems[rItem.iItem]->level, rItem.pszText, 10), rItem.cchTextMax);
+				_sntprintf_s(rItem.pszText, rItem.cchTextMax, _TRUNCATE, _T("%u"), m_ppIPFilterItems[rItem.iItem]->level);
 				rItem.pszText[rItem.cchTextMax - 1] = _T('\0');
 			}
 			break;
 		case IPFILTER_COL_HITS:
 			if (rItem.cchTextMax > 0) {
-				_tcsncpy(rItem.pszText, _itot(m_ppIPFilterItems[rItem.iItem]->hits, rItem.pszText, 10), rItem.cchTextMax);
+				_sntprintf_s(rItem.pszText, rItem.cchTextMax, _TRUNCATE, _T("%u"), m_ppIPFilterItems[rItem.iItem]->hits);
 				rItem.pszText[rItem.cchTextMax - 1] = _T('\0');
 			}
 			break;
diff --git a/srchybrid/Ini2.cpp b/srchybrid/Ini2.cpp
index 19a7822..c4141f9 100644
--- a/srchybrid/Ini2.cpp
+++ b/srchybrid/Ini2.cpp
@@ -14,7 +14,7 @@ static char THIS_FILE[] = __FILE__;
 //Input lines would be canonicalised: lines trimmed, spaces inside brackets and around equal sign removed
 bool CIni::Load()
 {
-	FILE *f = _tfopen(m_ininame, _T("r"));
+	FILE *f = _tfsopen(m_ininame, _T("r"), _SH_DENYNO);
 	if (!f)
 		return false;
 
@@ -61,7 +61,7 @@ bool CIni::Store()
 	if (!m_bWrite)
 		return true;
 
-	FILE *f = _tfopen(m_ininame + _T('t'), _T("w"));
+	FILE *f = _tfsopen(m_ininame + _T('t'), _T("w"), _SH_DENYNO);
 	if (f) {
 		for (INT_PTR i = 0; i < m_lines.GetCount(); ++i) {
 			fputs(m_lines[i], f);
@@ -183,7 +183,7 @@ void CIni::AddModulePath(CString &rstrFileName, bool bModulPath)
 	TCHAR fname[_MAX_FNAME];
 	TCHAR ext[_MAX_EXT];
 
-	_tsplitpath(rstrFileName, drive, dir, fname, ext);
+	_tsplitpath_s(rstrFileName, drive, _countof(drive), dir, _countof(dir), fname, _countof(fname), ext, _countof(ext));
 	if (!drive[0]) {
 		//PathCanonicalize(...) doesn't work on all Platforms!
 		CString strModule;
@@ -197,7 +197,7 @@ void CIni::AddModulePath(CString &rstrFileName, bool bModulPath)
 			strModule.TrimRight(_T("\\/"));
 			strModule += _T("\\");
 		}
-		_tsplitpath(strModule, drive, dir, fname, ext);
+		_tsplitpath_s(strModule, drive, _countof(drive), dir, _countof(dir), fname, _countof(fname), ext, _countof(ext));
 		strModule.Format(_T("%s%s%s"), drive, dir, (LPCTSTR)rstrFileName);
 		rstrFileName = strModule;
 	}
@@ -212,7 +212,7 @@ CString CIni::GetDefaultIniFile(bool bModulPath)
 	CString strTemp;
 	DWORD dwModPathLen = ::GetModuleFileName(NULL, strTemp.GetBuffer(MAX_PATH), MAX_PATH);
 	strTemp.ReleaseBuffer((dwModPathLen == 0 || dwModPathLen == MAX_PATH) ? 0 : -1);
-	_tsplitpath(strTemp, drive, dir, fname, ext);
+	_tsplitpath_s(strTemp, drive, _countof(drive), dir, _countof(dir), fname, _countof(fname), ext, _countof(ext));
 	strTemp.Format(_T("%s.ini"), fname);
 
 	CString strApplName;
@@ -423,7 +423,7 @@ void CIni::WriteUInt64(LPCTSTR lpszEntry, ULONGLONG n, LPCTSTR lpszSection)
 	if (lpszSection != NULL)
 		m_strSection = lpszSection;
 	TCHAR szBuffer[MAX_PATH];
-	_ui64tot(n, szBuffer, 10);
+	_ui64tot_s(n, szBuffer, _countof(szBuffer), 10);
 	Write(m_strSection, lpszEntry, szBuffer);
 }
 
diff --git a/srchybrid/KnownFile.cpp b/srchybrid/KnownFile.cpp
index 09c5b70..004b09f 100644
--- a/srchybrid/KnownFile.cpp
+++ b/srchybrid/KnownFile.cpp
@@ -358,7 +358,7 @@ bool CKnownFile::CreateFromFile(LPCTSTR in_directory, LPCTSTR in_filename, LPVOI
 	SetFilePath(strFilePath);
 	FILE *file = _tfsopen(strFilePath, _T("rbS"), _SH_DENYNO); // can not use _SH_DENYWR because we may access a completing part file
 	if (!file) {
-		LogError(GetResString(IDS_ERR_FILEOPEN) + _T(" - %s"), (LPCTSTR)strFilePath, _T(""), _tcserror(errno));
+		LogError(GetResString(IDS_ERR_FILEOPEN) + _T(" - %s"), (LPCTSTR)strFilePath, _T(""), (LPCTSTR)GetErrnoMessage(errno));
 		return false;
 	}
 
@@ -366,7 +366,7 @@ bool CKnownFile::CreateFromFile(LPCTSTR in_directory, LPCTSTR in_filename, LPVOI
 	__int64 llFileSize = _filelengthi64(_fileno(file));
 	if ((uint64)llFileSize > MAX_EMULE_FILE_SIZE) {
 		if (llFileSize <= 0)
-			LogError(_T("Failed to hash file \"%s\" - %s"), (LPCTSTR)strFilePath, _tcserror(errno));
+			LogError(_T("Failed to hash file \"%s\" - %s"), (LPCTSTR)strFilePath, (LPCTSTR)GetErrnoMessage(errno));
 		else
 			LogError(_T("Skipped hashing file \"%s\" - File size exceeds limit."), (LPCTSTR)strFilePath);
 		fclose(file);
@@ -396,7 +396,7 @@ bool CKnownFile::CreateFromFile(LPCTSTR in_directory, LPCTSTR in_filename, LPVOI
 
 		uchar *newhash = new uchar[MDX_DIGEST_SIZE];
 		if (!CreateHash(file, uSize, newhash, pBlockAICHHashTree)) {
-			LogError(_T("Failed to hash file \"%s\" - %s"), (LPCTSTR)strFilePath, _tcserror(errno));
+			LogError(_T("Failed to hash file \"%s\" - %s"), (LPCTSTR)strFilePath, (LPCTSTR)GetErrnoMessage(errno));
 			fclose(file);
 			delete[] newhash;
 			return false;
@@ -494,7 +494,7 @@ bool CKnownFile::CreateAICHHashSetOnly()
 
 	FILE *file = _tfsopen(GetFilePath(), _T("rbS"), _SH_DENYNO); // can not use _SH_DENYWR because we may access a completing part file
 	if (!file) {
-		LogError(GetResString(IDS_ERR_FILEOPEN) + _T(" - %s"), (LPCTSTR)GetFilePath(), _T(""), _tcserror(errno));
+		LogError(GetResString(IDS_ERR_FILEOPEN) + _T(" - %s"), (LPCTSTR)GetFilePath(), _T(""), (LPCTSTR)GetErrnoMessage(errno));
 		return false;
 	}
 	// we are reading the file data later in 8K blocks, adjust the internal file stream buffer accordingly
@@ -508,7 +508,7 @@ bool CKnownFile::CreateAICHHashSetOnly()
 		CAICHHashTree *pBlockAICHHashTree = cAICHHashSet.m_pHashTree.FindHash(hashcount * PARTSIZE, uSize);
 		ASSERT(pBlockAICHHashTree != NULL);
 		if (!CreateHash(file, uSize, NULL, pBlockAICHHashTree)) {
-			LogError(_T("Failed to hash file \"%s\" - %s"), (LPCTSTR)GetFilePath(), _tcserror(errno));
+			LogError(_T("Failed to hash file \"%s\" - %s"), (LPCTSTR)GetFilePath(), (LPCTSTR)GetErrnoMessage(errno));
 			fclose(file);
 			return false;
 		}
diff --git a/srchybrid/Log.cpp b/srchybrid/Log.cpp
index 5ea05e8..18559d3 100644
--- a/srchybrid/Log.cpp
+++ b/srchybrid/Log.cpp
@@ -382,14 +382,16 @@ void CLogFile::StartNewLogFile()
 	time_t tStarted = m_tStarted;
 	Close();
 
+	struct tm tmStarted;
+	localtime_s(&tmStarted, &tStarted);
 	TCHAR szDateLogStarted[40];
-	_tcsftime(szDateLogStarted, _countof(szDateLogStarted), _T("%Y.%m.%d %H.%M.%S"), localtime(&tStarted));
+	_tcsftime(szDateLogStarted, _countof(szDateLogStarted), _T("%Y.%m.%d %H.%M.%S"), &tmStarted);
 
 	TCHAR szDrv[_MAX_DRIVE];
 	TCHAR szDir[_MAX_DIR];
 	TCHAR szNam[_MAX_FNAME];
 	TCHAR szExt[_MAX_EXT];
-	_tsplitpath(m_strFilePath, szDrv, szDir, szNam, szExt);
+	_tsplitpath_s(m_strFilePath, szDrv, _countof(szDrv), szDir, _countof(szDir), szNam, _countof(szNam), szExt, _countof(szExt));
 
 	CString strLogBakNam;
 	strLogBakNam.Format(_T("%s - %s"), szNam, szDateLogStarted);
diff --git a/srchybrid/MediaInfo.cpp b/srchybrid/MediaInfo.cpp
index e5f5eaa..7b36fe6 100644
--- a/srchybrid/MediaInfo.cpp
+++ b/srchybrid/MediaInfo.cpp
@@ -621,7 +621,8 @@ bool GetRIFFHeaders(LPCTSTR pszFileName, SMediaInfo *mi, bool &rbIsAVI, bool bFu
 	ASSERT(!bFullInfo || mi->strInfo.m_hWnd);
 
 	// Open AVI file
-	int hAviFile = _topen(pszFileName, O_RDONLY | O_BINARY);
+	int hAviFile;
+	_tsopen_s(&hAviFile, pszFileName, O_RDONLY | O_BINARY, _SH_DENYNO, 0);
 	if (hAviFile < 0)
 		return false;
 
@@ -2737,7 +2738,8 @@ bool GetWMHeaders(LPCTSTR pszFileName, SMediaInfo *mi, bool &rbIsWM, bool bFullI
 
 bool GetMimeType(LPCTSTR pszFilePath, CString &rstrMimeType)
 {
-	int fd = _topen(pszFilePath, O_RDONLY | O_BINARY);
+	int fd;
+	_tsopen_s(&fd, pszFilePath, O_RDONLY | O_BINARY, _SH_DENYNO, 0);
 	if (fd != -1) {
 		BYTE buffer[8192];
 		int iRead = _read(fd, buffer, sizeof buffer);
diff --git a/srchybrid/OScopeCtrl.cpp b/srchybrid/OScopeCtrl.cpp
index a744157..6f22780 100644
--- a/srchybrid/OScopeCtrl.cpp
+++ b/srchybrid/OScopeCtrl.cpp
@@ -875,8 +875,10 @@ void COScopeCtrl::OnMouseMove(UINT nFlags, CPoint point)
 
 		DWORD dwTime = (DWORD)((float)mypos * apixel);
 		time_t tNow = time(NULL) - dwTime;
+		struct tm tmNow;
+		localtime_s(&tmNow, &tNow);
 		TCHAR szDate[128];
-		_tcsftime(szDate, _countof(szDate), thePrefs.GetDateTimeFormat4Log(), localtime(&tNow));
+		_tcsftime(szDate, _countof(szDate), thePrefs.GetDateTimeFormat4Log(), &tmNow);
 		CString strInfo(m_str.YUnits);
 		strInfo.AppendFormat(_T(": %u @ %s ") + GetResString(IDS_TIMEBEFORE), yValue, szDate, (LPCTSTR)CastSecondsToLngHM(dwTime));
 
diff --git a/srchybrid/OtherFunctions.cpp b/srchybrid/OtherFunctions.cpp
index 23728a1..cb98334 100644
--- a/srchybrid/OtherFunctions.cpp
+++ b/srchybrid/OtherFunctions.cpp
@@ -2155,7 +2155,7 @@ CString GetFormatedUInt(ULONG ulVal)
 CString GetFormatedUInt64(ULONGLONG ullVal)
 {
 	TCHAR szVal[24];
-	_ui64tot(ullVal, szVal, 10);
+	_ui64tot_s(ullVal, szVal, _countof(szVal), 10);
 
 	static NUMBERFMT nf;
 	if (nf.Grouping == 0) {
@@ -2200,7 +2200,9 @@ void Debug(LPCTSTR pszFmtMsg, ...)
 	CString strBuff;
 #ifdef _DEBUG
 	time_t tNow = time(NULL);
-	int iTimeLen = (int)_tcsftime(strBuff.GetBuffer(40), 40, _T("%H:%M:%S "), localtime(&tNow));
+	struct tm tmNow;
+	localtime_s(&tmNow, &tNow);
+	int iTimeLen = (int)_tcsftime(strBuff.GetBuffer(40), 40, _T("%H:%M:%S "), &tmNow);
 	strBuff.ReleaseBuffer(iTimeLen);
 #endif
 	strBuff.AppendFormatV(pszFmtMsg, pArgs);
@@ -2734,6 +2736,15 @@ uint32 IPv4FromStringA(LPCSTR pszAddress)
 	return (iResult == 0) ? sa.sin_addr.s_addr : INADDR_NONE;
 }
 
+CString GetErrnoMessage(int iErrno)
+{
+	// the text _tcserror() (deprecated) returned, copied out of the CRT's buffer
+	TCHAR szMsg[256];
+	if (_tcserror_s(szMsg, _countof(szMsg), iErrno) != 0)
+		*szMsg = _T('\0');
+	return CString(szMsg);
+}
+
 static bool IsDaylightSavingTimeActive(LONG &rlDaylightBias)
 {
 	TIME_ZONE_INFORMATION tzi;
@@ -3767,7 +3778,8 @@ static const byte FILEHEADER_ZIP_ID[] = { 0x50, 0x4b, 0x03, 0x04 };
 
 bool GetDRM(LPCTSTR pszFilePath)
 {
-	int fd = _topen(pszFilePath, O_RDONLY | O_BINARY);
+	int fd;
+	_tsopen_s(&fd, pszFilePath, O_RDONLY | O_BINARY, _SH_DENYNO, 0);
 	if (fd != -1) {
 		BYTE aucBuff[16384];
 		int iRead = _read(fd, aucBuff, sizeof aucBuff);
diff --git a/srchybrid/OtherFunctions.h b/srchybrid/OtherFunctions.h
index f45fba5..dfecf16 100644
--- a/srchybrid/OtherFunctions.h
+++ b/srchybrid/OtherFunctions.h
@@ -489,6 +489,7 @@ CString ipstr(LPCTSTR pszAddress, uint16 nPort);
 CStringA ipstrA(uint32 nIP);
 void ipstrA(CHAR *pszAddress, int iMaxAddress, uint32 nIP);
 uint32 IPv4FromStringA(LPCSTR pszAddress);
+CString GetErrnoMessage(int iErrno);
 inline CString ipstr(in_addr nIP)
 {
 	return ipstr(*(uint32*)&nIP);
diff --git a/srchybrid/PPgSecurity.cpp b/srchybrid/PPgSecurity.cpp
index 671f38c..bb1ec51 100644
--- a/srchybrid/PPgSecurity.cpp
+++ b/srchybrid/PPgSecurity.cpp
@@ -249,11 +249,11 @@ void CPPgSecurity::OnLoadIPFFromURL()
 					zip.Close();
 
 					if (_tremove(theApp.ipfilter->GetDefaultFilePath()) != 0)
-						TRACE(_T("*** Error: Failed to remove default IP filter file \"%s\" - %s\n"), (LPCTSTR)theApp.ipfilter->GetDefaultFilePath(), _tcserror(errno));
+						TRACE(_T("*** Error: Failed to remove default IP filter file \"%s\" - %s\n"), (LPCTSTR)theApp.ipfilter->GetDefaultFilePath(), (LPCTSTR)GetErrnoMessage(errno));
 					if (_trename(strTempUnzipFilePath, theApp.ipfilter->GetDefaultFilePath()) != 0)
-						TRACE(_T("*** Error: Failed to rename uncompressed IP filter file \"%s\" to default IP filter file \"%s\" - %s\n"), (LPCTSTR)strTempUnzipFilePath, (LPCTSTR)theApp.ipfilter->GetDefaultFilePath(), _tcserror(errno));
+						TRACE(_T("*** Error: Failed to rename uncompressed IP filter file \"%s\" to default IP filter file \"%s\" - %s\n"), (LPCTSTR)strTempUnzipFilePath, (LPCTSTR)theApp.ipfilter->GetDefaultFilePath(), (LPCTSTR)GetErrnoMessage(errno));
 					if (_tremove(strTempFilePath) != 0)
-						TRACE(_T("*** Error: Failed to remove temporary IP filter file \"%s\" - %s\n"), (LPCTSTR)strTempFilePath, _tcserror(errno));
+						TRACE(_T("*** Error: Failed to remove temporary IP filter file \"%s\" - %s\n"), (LPCTSTR)strTempFilePath, (LPCTSTR)GetErrnoMessage(errno));
 					bUncompressed = true;
 					bHaveNewFilterFile = true;
 				} else {
@@ -286,11 +286,11 @@ void CPPgSecurity::OnLoadIPFFromURL()
 						rar.Close();
 
 						if (_tremove(theApp.ipfilter->GetDefaultFilePath()) != 0)
-							TRACE(_T("*** Error: Failed to remove default IP filter file \"%s\" - %s\n"), (LPCTSTR)theApp.ipfilter->GetDefaultFilePath(), _tcserror(errno));
+							TRACE(_T("*** Error: Failed to remove default IP filter file \"%s\" - %s\n"), (LPCTSTR)theApp.ipfilter->GetDefaultFilePath(), (LPCTSTR)GetErrnoMessage(errno));
 						if (_trename(strTempUnzipFilePath, theApp.ipfilter->GetDefaultFilePath()) != 0)
-							TRACE(_T("*** Error: Failed to rename uncompressed IP filter file \"%s\" to default IP filter file \"%s\" - %s\n"), (LPCTSTR)strTempUnzipFilePath, (LPCTSTR)theApp.ipfilter->GetDefaultFilePath(), _tcserror(errno));
+							TRACE(_T("*** Error: Failed to rename uncompressed IP filter file \"%s\" to default IP filter file \"%s\" - %s\n"), (LPCTSTR)strTempUnzipFilePath, (LPCTSTR)theApp.ipfilter->GetDefaultFilePath(), (LPCTSTR)GetErrnoMessage(errno));
 						if (_tremove(strTempFilePath) != 0)
-							TRACE(_T("*** Error: Failed to remove temporary IP filter file \"%s\" - %s\n"), (LPCTSTR)strTempFilePath, _tcserror(errno));
+							TRACE(_T("*** Error: Failed to remove temporary IP filter file \"%s\" - %s\n"), (LPCTSTR)strTempFilePath, (LPCTSTR)GetErrnoMessage(errno));
 						bUncompressed = true;
 						bHaveNewFilterFile = true;
 					} else {
@@ -327,11 +327,11 @@ void CPPgSecurity::OnLoadIPFFromURL()
 					gz.Close();
 
 					if (_tremove(theApp.ipfilter->GetDefaultFilePath()) != 0)
-						TRACE(_T("*** Error: Failed to remove default IP filter file \"%s\" - %s\n"), (LPCTSTR)theApp.ipfilter->GetDefaultFilePath(), _tcserror(errno));
+						TRACE(_T("*** Error: Failed to remove default IP filter file \"%s\" - %s\n"), (LPCTSTR)theApp.ipfilter->GetDefaultFilePath(), (LPCTSTR)GetErrnoMessage(errno));
 					if (_trename(strTempUnzipFilePath, theApp.ipfilter->GetDefaultFilePath()) != 0)
-						TRACE(_T("*** Error: Failed to rename uncompressed IP filter file \"%s\" to default IP filter file \"%s\" - %s\n"), (LPCTSTR)strTempUnzipFilePath, (LPCTSTR)theApp.ipfilter->GetDefaultFilePath(), _tcserror(errno));
+						TRACE(_T("*** Error: Failed to rename uncompressed IP filter file \"%s\" to default IP filter file \"%s\" - %s\n"), (LPCTSTR)strTempUnzipFilePath, (LPCTSTR)theApp.ipfilter->GetDefaultFilePath(), (LPCTSTR)GetErrnoMessage(errno));
 					if (_tremove(strTempFilePath) != 0)
-						TRACE(_T("*** Error: Failed to remove temporary IP filter file \"%s\" - %s\n"), (LPCTSTR)strTempFilePath, _tcserror(errno));
+						TRACE(_T("*** Error: Failed to remove temporary IP filter file \"%s\" - %s\n"), (LPCTSTR)strTempFilePath, (LPCTSTR)GetErrnoMessage(errno));
 					bUncompressed = true;
 					bHaveNewFilterFile = true;
 				} else {
diff --git a/srchybrid/PPgWebServer.cpp b/srchybrid/PPgWebServer.cpp
index cafbb8a..188fbf4 100644
--- a/srchybrid/PPgWebServer.cpp
+++ b/srchybrid/PPgWebServer.cpp
@@ -49,7 +49,7 @@ struct options
 
 static int write_buffer(LPCTSTR output_file, const unsigned char *buffer)
 {
-	FILE *f = _tfopen(output_file, _T("wb"));
+	FILE *f = _tfsopen(output_file, _T("wb"), _SH_DENYNO);
 	if (f == NULL)
 		return -1;
 	size_t len = strlen((char*)buffer);
diff --git a/srchybrid/PartFile.cpp b/srchybrid/PartFile.cpp
index b5ce3e3..1a5354a 100644
--- a/srchybrid/PartFile.cpp
+++ b/srchybrid/PartFile.cpp
@@ -428,7 +428,7 @@ void CPartFile::CreatePartFile(UINT cat)
 		}
 	} else {
 		m_tCreated = m_tLastModified = time(NULL);
-		AddDebugLogLine(false, _T("Failed to get file date for \"%s\" - %s"), (LPCTSTR)partfull, _tcserror(errno));
+		AddDebugLogLine(false, _T("Failed to get file date for \"%s\" - %s"), (LPCTSTR)partfull, (LPCTSTR)GetErrnoMessage(errno));
 	}
 
 	m_dwFileAttributes = ::GetFileAttributes(partfull);
@@ -1066,7 +1066,7 @@ EPartFileLoadResult CPartFile::LoadPartFile(LPCTSTR in_directory, LPCTSTR in_fil
 		m_tCreated = (time_t)st.st_ctime;
 		m_tLastModified = (time_t)st.st_mtime;
 	} else
-		AddDebugLogLine(false, _T("Failed to get file date for \"%s\" - %s"), (LPCTSTR)searchpath, _tcserror(errno));
+		AddDebugLogLine(false, _T("Failed to get file date for \"%s\" - %s"), (LPCTSTR)searchpath, (LPCTSTR)GetErrnoMessage(errno));
 
 	try {
 		SetFilePath(searchpath);
@@ -1372,7 +1372,7 @@ bool CPartFile::SavePartFile(bool bDontOverrideBak)
 		UINT i_pos = 0;
 		for (POSITION pos = m_gaplist.GetHeadPosition(); pos != NULL;) {
 			const Gap_Struct &gap = m_gaplist.GetNext(pos);
-			_itoa(i_pos, number, 10);
+			_itoa_s(i_pos, number, _countof(namebuffer) - 1, 10);
 			namebuffer[0] = FT_GAPSTART;
 			CTag gapstarttag(namebuffer, gap.start, IsLargeFile());
 			gapstarttag.WriteTagToFile(file);
@@ -1415,7 +1415,7 @@ bool CPartFile::SavePartFile(bool bDontOverrideBak)
 				nEnd = item->end;
 			}
 
-			_itoa(i_pos, number, 10);
+			_itoa_s(i_pos, number, _countof(namebuffer) - 1, 10);
 			namebuffer[0] = FT_GAPSTART;
 			CTag gapstarttag(namebuffer, nStart, IsLargeFile());
 			gapstarttag.WriteTagToFile(file);
@@ -3051,14 +3051,14 @@ void CPartFile::DeletePartFile()
 	static LPCTSTR const pszErrfmt = _T(" - %s");
 	if (_tremove(m_fullname)) {
 		CString sFmt(GetResString(IDS_ERR_DELETE));
-		sFmt.AppendFormat(pszErrfmt, _tcserror(errno));
+		sFmt.AppendFormat(pszErrfmt, (LPCTSTR)GetErrnoMessage(errno));
 		LogError(LOG_STATUSBAR, sFmt, (LPCTSTR)m_fullname);
 	}
 
 	const CString &partfilename(RemoveFileExtension(m_fullname));
 	if (_tremove(partfilename)) {
 		CString sFmt(GetResString(IDS_ERR_DELETE));
-		sFmt.AppendFormat(pszErrfmt, _tcserror(errno));
+		sFmt.AppendFormat(pszErrfmt, (LPCTSTR)GetErrnoMessage(errno));
 		LogError(LOG_STATUSBAR, sFmt, (LPCTSTR)partfilename);
 	}
 
@@ -3528,7 +3528,7 @@ bool CPartFile::IsReadyForPreview() const
 			&& IsComplete(GetPartCount() - 1);
 
 	TCHAR szVideoPlayerFileName[_MAX_FNAME];
-	_tsplitpath(thePrefs.GetVideoPlayer(), NULL, NULL, szVideoPlayerFileName, NULL);
+	_tsplitpath_s(thePrefs.GetVideoPlayer(), NULL, 0, NULL, 0, szVideoPlayerFileName, _countof(szVideoPlayerFileName), NULL, 0);
 
 	// enable the preview command if 'PreviewSmallBlocks' option is enabled
 	// or if VideoLAN client is specified
diff --git a/srchybrid/PerfLog.cpp b/srchybrid/PerfLog.cpp
index 07918b4..f4e5810 100644
--- a/srchybrid/PerfLog.cpp
+++ b/srchybrid/PerfLog.cpp
@@ -69,7 +69,7 @@ void CPerfLog::Startup()
 			TCHAR drv[_MAX_DRIVE];
 			TCHAR dir[_MAX_DIR];
 			TCHAR nam[_MAX_FNAME];
-			_tsplitpath(m_strFilePath, drv, dir, nam, NULL);
+			_tsplitpath_s(m_strFilePath, drv, _countof(drv), dir, _countof(dir), nam, _countof(nam), NULL, 0);
 			m_strFilePath.Empty();
 
 			_tmakepathlimit(m_strMRTGDataFilePath.GetBuffer(MAX_PATH), drv, dir, CString(nam) + _T("_data"), _T("mrtg"));
@@ -96,13 +96,15 @@ void CPerfLog::WriteSamples(UINT nCurDn, UINT nCurUp, UINT nCurDnOH, UINT nCurUp
 
 	if (m_eFileFormat == CSV) {
 		time_t tNow = time(NULL);
+		struct tm tmNow;
+		localtime_s(&tmNow, &tNow);
 		char szTime[40];
 		// do not localize this date/time string!
-		strftime(szTime, _countof(szTime), "%m/%d/%Y %H:%M:%S", localtime(&tNow));
+		strftime(szTime, _countof(szTime), "%m/%d/%Y %H:%M:%S", &tmNow);
 
 		FILE *fp = _tfsopen(m_strFilePath, (m_eMode == OneSample) ? _T("wt") : _T("at"), _SH_DENYWR);
 		if (fp == NULL) {
-			LogError(LOG_DEFAULT, _T("Failed to open performance log file \"%s\" - %s"), (LPCTSTR)m_strFilePath, _tcserror(errno));
+			LogError(LOG_DEFAULT, _T("Failed to open performance log file \"%s\" - %s"), (LPCTSTR)m_strFilePath, (LPCTSTR)GetErrnoMessage(errno));
 			return;
 		}
 		::setvbuf(fp, NULL, _IOFBF, 16384); // ensure that all lines are written to file with one call
@@ -118,14 +120,14 @@ void CPerfLog::WriteSamples(UINT nCurDn, UINT nCurUp, UINT nCurDnOH, UINT nCurUp
 			fprintf(fp, "%u\n%u\n\n\n", nCurDn, nCurUp);
 			fclose(fp);
 		} else
-			LogError(LOG_DEFAULT, _T("Failed to open performance log file \"%s\" - %s"), (LPCTSTR)m_strMRTGDataFilePath, _tcserror(errno));
+			LogError(LOG_DEFAULT, _T("Failed to open performance log file \"%s\" - %s"), (LPCTSTR)m_strMRTGDataFilePath, (LPCTSTR)GetErrnoMessage(errno));
 
 		fp = _tfsopen(m_strMRTGOverheadFilePath, (m_eMode == OneSample) ? _T("wt") : _T("at"), _SH_DENYWR);
 		if (fp != NULL) {
 			fprintf(fp, "%u\n%u\n\n\n", nCurDnOH, nCurUpOH);
 			fclose(fp);
 		} else
-			LogError(LOG_DEFAULT, _T("Failed to open performance log file \"%s\" - %s"), (LPCTSTR)m_strMRTGOverheadFilePath, _tcserror(errno));
+			LogError(LOG_DEFAULT, _T("Failed to open performance log file \"%s\" - %s"), (LPCTSTR)m_strMRTGOverheadFilePath, (LPCTSTR)GetErrnoMessage(errno));
 	}
 }
 
diff --git a/srchybrid/Preferences.cpp b/srchybrid/Preferences.cpp
index 5915205..3ca8209 100644
--- a/srchybrid/Preferences.cpp
+++ b/srchybrid/Preferences.cpp
@@ -1378,12 +1378,11 @@ CString CPreferences::GetStatsLastResetStr(bool formatLong)
 	// true: DateTime format from the .ini
 	// false: DateTime format from the .ini for the log
 	if (GetStatsLastResetLng()) {
-		tm *statsReset;
+		tm statsReset;
 		TCHAR szDateReset[128];
 		time_t lastResetDateTime = GetStatsLastResetLng();
-		statsReset = localtime(&lastResetDateTime);
-		if (statsReset) {
-			_tcsftime(szDateReset, _countof(szDateReset), formatLong ? (LPCTSTR)GetDateTimeFormat() : _T("%c"), statsReset);
+		if (localtime_s(&statsReset, &lastResetDateTime) == 0) {
+			_tcsftime(szDateReset, _countof(szDateReset), formatLong ? (LPCTSTR)GetDateTimeFormat() : _T("%c"), &statsReset);
 			if (*szDateReset)
 				return CString(szDateReset);
 		}
diff --git a/srchybrid/SendMail.cpp b/srchybrid/SendMail.cpp
index 6714843..a30c205 100644
--- a/srchybrid/SendMail.cpp
+++ b/srchybrid/SendMail.cpp
@@ -115,7 +115,7 @@ bool Encrypt(const CStringA &rstrContentA, CByteArray &raEncrypted, LPCWSTR pwsz
 					}
 				} catch (CMemoryException *ex) {
 					ex->Delete();
-					DebugLogWarning(LOG_DONTNOTIFY, _T("Email Encryption: Failed to encrypt message - %s"), _tcserror(ENOMEM));
+					DebugLogWarning(LOG_DONTNOTIFY, _T("Email Encryption: Failed to encrypt message - %s"), (LPCTSTR)GetErrnoMessage(ENOMEM));
 				}
 			} else
 				DebugLogWarning(LOG_DONTNOTIFY, _T("Email Encryption: Failed to get length of encrypted message - %s"), (LPCTSTR)GetErrorMessage(::GetLastError(), 1));
diff --git a/srchybrid/SharedFileList.cpp b/srchybrid/SharedFileList.cpp
index faac858..a6bf3df 100644
--- a/srchybrid/SharedFileList.cpp
+++ b/srchybrid/SharedFileList.cpp
@@ -325,9 +325,12 @@ bool CAddFileThread::ImportParts()
 		return false;
 	}
 
-	CString strFilePath;
-	_tmakepath(strFilePath.GetBuffer(MAX_PATH), NULL, m_strDirectory, m_strFilename, NULL);
-	strFilePath.ReleaseBuffer();
+	// what _tmakepath() (deprecated) built, without its MAX_PATH buffer: the directory,
+	// a separator unless it already ends in one, and the file name
+	CString strFilePath(m_strDirectory);
+	if (!strFilePath.IsEmpty() && strFilePath.Right(1) != _T("\\") && strFilePath.Right(1) != _T("/"))
+		strFilePath += _T('\\');
+	strFilePath += m_strFilename;
 
 	Log(LOG_STATUSBAR, GetResString(IDS_IMPORTPARTS_IMPORTSTART), m_PartsToImport.GetSize(), (LPCTSTR)strFilePath);
 
diff --git a/srchybrid/SharedFilesCtrl.cpp b/srchybrid/SharedFilesCtrl.cpp
index f538ba9..594fc89 100644
--- a/srchybrid/SharedFilesCtrl.cpp
+++ b/srchybrid/SharedFilesCtrl.cpp
@@ -901,7 +901,7 @@ BOOL CSharedFilesCtrl::OnCommand(WPARAM wParam, LPARAM)
 					newpath.ReleaseBuffer();
 					if (_trename(pKnownFile->GetFilePath(), newpath) != 0) {
 						CString strError;
-						strError.Format(GetResString(IDS_ERR_RENAMESF), (LPCTSTR)pKnownFile->GetFilePath(), (LPCTSTR)newpath, _tcserror(errno));
+						strError.Format(GetResString(IDS_ERR_RENAMESF), (LPCTSTR)pKnownFile->GetFilePath(), (LPCTSTR)newpath, (LPCTSTR)GetErrnoMessage(errno));
 						AfxMessageBox(strError);
 						break;
 					}
diff --git a/srchybrid/WebServer.cpp b/srchybrid/WebServer.cpp
index ff6e700..befe768 100644
--- a/srchybrid/WebServer.cpp
+++ b/srchybrid/WebServer.cpp
@@ -152,7 +152,8 @@ bool CWebServer::ReloadTemplates()
 	TCHAR szTime[32];
 	time_t t = time(NULL);
 	_locale_t locale = _tcreate_locale(LC_TIME, _T("en-US"));
-	if (!_tcsftime_l(szTime, _countof(szTime), _T("%a, %d %b %Y %H:%M:%S GMT"), gmtime(&t), locale))
+	struct tm tmGmt;
+	if (gmtime_s(&tmGmt, &t) != 0 || !_tcsftime_l(szTime, _countof(szTime), _T("%a, %d %b %Y %H:%M:%S GMT"), &tmGmt, locale))
 		*szTime = _T('\0');
 	if (locale)
 		_free_locale(locale);
