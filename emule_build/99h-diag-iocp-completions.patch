diff --git a/srchybrid/PartFileWriteThread.cpp b/srchybrid/PartFileWriteThread.cpp
index b3492f5..b5eabac 100644
--- a/srchybrid/PartFileWriteThread.cpp
+++ b/srchybrid/PartFileWriteThread.cpp
@@ -38,6 +38,8 @@ CPartFileWriteThread::CPartFileWriteThread()
 	: m_eventThreadEnded(FALSE, TRUE)
 	, m_hPort()
 	, m_Run(RUN_STOP)
+	, m_nDiagTakeFailed() //UPDIAG
+	, m_dwDiagLastFailLog() //UPDIAG
 	, m_bNewData()
 {
 	AfxBeginThread(RunProc, (LPVOID)this, THREAD_PRIORITY_BELOW_NORMAL);
@@ -73,8 +75,23 @@ bool CPartFileWriteThread::TakeCompletion(DWORD dwTimeout, DWORD &rdwWritten, UL
 	rpIO = NULL;
 	if (::GetQueuedCompletionStatus(m_hPort, &rdwWritten, &rKey, (LPOVERLAPPED*)&rpIO, dwTimeout))
 		return true;
+	const DWORD dwDiagErr = ::GetLastError(); //UPDIAG - read before anything else can clear it
 	rdwWritten = 0;
-	return rpIO != NULL;
+	if (rpIO == NULL)
+		return false;
+	//UPDIAG: a write that failed, taken and accounted for. Before 97i this ended the loop
+	//that took it, which on this thread means downloaded data stops reaching the disk.
+	++m_nDiagTakeFailed;
+	{
+		const DWORD dwDiagNow = ::GetTickCount();
+		if (dwDiagNow - m_dwDiagLastFailLog >= 1000) {
+			m_dwDiagLastFailLog = dwDiagNow;
+			theApp.QueueDebugLogLineEx(LOG_ERROR
+				, _T("UPDIAG|writethread took a FAILED write completion: err=%u pending=%d takefail=%ld")
+				, dwDiagErr, (int)m_listPendingIO.GetCount(), (LONG)m_nDiagTakeFailed);
+		}
+	}
+	return true;
 }
 
 UINT CPartFileWriteThread::RunInternal()
@@ -118,6 +135,12 @@ UINT CPartFileWriteThread::RunInternal()
 		if (InterlockedExchange8(&m_bNewData, 0) && m_listPendingIO.IsEmpty())
 			PostQueuedCompletionStatus(m_hPort, 0, WAKEUP, NULL);
 	}
+	//UPDIAG: the twin of the line in UploadDiskIOThread - this thread ending is how
+	//downloaded data stops reaching the disk, and it used to end without a word.
+	theApp.QueueDebugLogLineEx(LOG_ERROR
+		, _T("UPDIAG|writethread RunInternal leaving: run=%d stoppacket=%d key=%I64u pending=%d takefail=%ld")
+		, (int)m_Run, (int)bStopPacket, (ULONGLONG)completionKey, (int)m_listPendingIO.GetCount()
+		, (LONG)m_nDiagTakeFailed);
 	m_Run = RUN_STOP;
 
 	//Improper termination of asynchronous I/O follows...
diff --git a/srchybrid/PartFileWriteThread.h b/srchybrid/PartFileWriteThread.h
index a1a514d..310f5fd 100644
--- a/srchybrid/PartFileWriteThread.h
+++ b/srchybrid/PartFileWriteThread.h
@@ -64,6 +64,11 @@ private:
 
 	CEvent	m_eventThreadEnded;
 	HANDLE	m_hPort;
+	// UPDIAG: the write side gets the two lines that matter and no reporting surface of
+	// its own - nothing outside this class reads its state, so counters would have no
+	// reader. A failed write completion and an unexpected exit both write to the log.
+	volatile LONG m_nDiagTakeFailed;
+	DWORD m_dwDiagLastFailLog;
 	volatile char m_Run; //0 - not running; 1 - idle; 2 - processing
 	volatile char m_bNewData;
 };
\ No newline at end of file
diff --git a/srchybrid/UploadDiskIOThread.cpp b/srchybrid/UploadDiskIOThread.cpp
index 73afd59..3feb30c 100644
--- a/srchybrid/UploadDiskIOThread.cpp
+++ b/srchybrid/UploadDiskIOThread.cpp
@@ -74,6 +74,11 @@ CUploadDiskIOThread::CUploadDiskIOThread()
 	, m_nDiagNoBlocks()
 	, m_nDiagNoSocket()
 	, m_nDiagBuffered()
+	, m_nDiagTakeOk() //UPDIAG
+	, m_nDiagTakeFailed() //UPDIAG
+	, m_nDiagTakeNone() //UPDIAG
+	, m_dwDiagLastTake() //UPDIAG
+	, m_dwDiagLastFailLog() //UPDIAG
 {
 	//UPDIAG
 	memset((void*)m_anDiagWakeAsked, 0, sizeof m_anDiagWakeAsked);
@@ -119,10 +124,32 @@ bool CUploadDiskIOThread::TakeCompletion(DWORD dwTimeout, DWORD &rdwRead, ULONG_
 	rdwRead = 0;
 	rKey = 0;
 	rpIO = NULL;
-	if (::GetQueuedCompletionStatus(m_hPort, &rdwRead, &rKey, (LPOVERLAPPED*)&rpIO, dwTimeout))
+	if (::GetQueuedCompletionStatus(m_hPort, &rdwRead, &rKey, (LPOVERLAPPED*)&rpIO, dwTimeout)) {
+		++m_nDiagTakeOk; //UPDIAG
+		::InterlockedExchange(&m_dwDiagLastTake, (LONG)::GetTickCount()); //UPDIAG
 		return true;
+	}
+	const DWORD dwDiagErr = ::GetLastError(); //UPDIAG - read before anything else can clear it
 	rdwRead = 0;
-	return rpIO != NULL;
+	if (rpIO == NULL) {
+		++m_nDiagTakeNone; //UPDIAG
+		return false;
+	}
+	//UPDIAG: the event the correction above exists for. It used to end the loop that took
+	//it - the drain losing one read for good, the outer wait losing this thread - and say
+	//nothing at all. One line a second is enough to see it arrive without flooding.
+	++m_nDiagTakeFailed;
+	::InterlockedExchange(&m_dwDiagLastTake, (LONG)::GetTickCount());
+	{
+		const DWORD dwDiagNow = ::GetTickCount();
+		if (dwDiagNow - m_dwDiagLastFailLog >= 1000) {
+			m_dwDiagLastFailLog = dwDiagNow;
+			theApp.QueueDebugLogLineEx(LOG_ERROR
+				, _T("UPDIAG|iothread took a FAILED read completion: err=%u pending=%d takefail=%ld")
+				, dwDiagErr, (int)m_listPendingIO.GetCount(), (LONG)m_nDiagTakeFailed);
+		}
+	}
+	return true;
 }
 
 UINT CUploadDiskIOThread::RunInternal()
@@ -193,6 +220,13 @@ UINT CUploadDiskIOThread::RunInternal()
 			theApp.QueueDebugLogLineEx(LOG_INFO, _T("UPDIAG|iothread %s"), (LPCTSTR)DbgGetDiagInfo());
 		}
 	}
+	//UPDIAG: this thread feeds every upload and could end without a word. Say which of the
+	//three ways out it took and what was still pending, so a run that stops uploading has
+	//an entry to be read against instead of silence.
+	theApp.QueueDebugLogLineEx(LOG_ERROR
+		, _T("UPDIAG|iothread RunInternal leaving: run=%d stoppacket=%d key=%I64u pending=%d | %s")
+		, (int)m_Run, (int)bStopPacket, (ULONGLONG)completionKey, (int)m_listPendingIO.GetCount()
+		, (LPCTSTR)DbgGetDiagInfo());
 	m_Run = RUN_STOP;
 
 	//Improper termination of asynchronous I/O follows...
@@ -213,10 +247,13 @@ CString CUploadDiskIOThread::DbgGetDiagInfo() const
 	CString strInfo;
 	strInfo.Format(_T("run=%d newdata=%d pending=%d pendmax=%ld loops=%ld wakeups=%ld outerio=%ld")
 		_T(" drained=%ld reads=%ld zeroreads=%ld noblocks=%ld nosock=%ld buffered=%ld")
+		_T(" takeok=%ld takefail=%ld takenone=%ld takeage=%ums")
 		, (int)m_Run, (int)m_bNewData, (int)m_listPendingIO.GetCount(), (LONG)m_nDiagPendMax
 		, (LONG)m_nDiagLoops, (LONG)m_nDiagWakeups, (LONG)m_nDiagOuterIO
 		, (LONG)m_nDiagDrained, (LONG)m_nDiagReads, (LONG)m_nDiagZeroReads
-		, (LONG)m_nDiagNoBlocks, (LONG)m_nDiagNoSocket, (LONG)m_nDiagBuffered);
+		, (LONG)m_nDiagNoBlocks, (LONG)m_nDiagNoSocket, (LONG)m_nDiagBuffered
+		, (LONG)m_nDiagTakeOk, (LONG)m_nDiagTakeFailed, (LONG)m_nDiagTakeNone
+		, (DWORD)(m_dwDiagLastTake ? ::GetTickCount() - (DWORD)m_dwDiagLastTake : 0));
 	//UPDIAG: asked/merged/taken/work per caller, in the order of EDiagWakeSrc. The last number
 	//is the only one that says the wakeup was worth posting.
 	static LPCTSTR const apszSrc[DIAGWAKE_COUNT] = { _T("newdata"), _T("reqblock"), _T("nodata"), _T("needmore") };
diff --git a/srchybrid/UploadDiskIOThread.h b/srchybrid/UploadDiskIOThread.h
index 80abdc1..5a21ee3 100644
--- a/srchybrid/UploadDiskIOThread.h
+++ b/srchybrid/UploadDiskIOThread.h
@@ -88,6 +88,16 @@ private:
 	volatile LONG m_nDiagNoBlocks;
 	volatile LONG m_nDiagNoSocket;
 	volatile LONG m_nDiagBuffered;
+	// UPDIAG: the three answers TakeCompletion() can give, counted where they are given.
+	// takefail is the one that matters: before 97i a dequeued failed completion ended
+	// whichever loop took it, silently. takeage says when a completion last arrived, and
+	// is read from another thread through DbgGetDiagInfo(), which is the only way to see
+	// this thread stop - once it is gone it cannot report anything itself.
+	volatile LONG m_nDiagTakeOk;
+	volatile LONG m_nDiagTakeFailed;
+	volatile LONG m_nDiagTakeNone;
+	volatile LONG m_dwDiagLastTake;
+	DWORD m_dwDiagLastFailLog;
 	// UPDIAG: per caller - how often it asked, how often the ask reached the port rather than
 	// being folded into m_bNewData, how many passes it actually caused, and in how many of
 	// those a read was started. A caller whose work count stays at zero wakes this thread for
