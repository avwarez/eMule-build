diff --git a/srchybrid/Stdafx.h b/srchybrid/Stdafx.h
index 4e980f9..5c14c7c 100644
--- a/srchybrid/Stdafx.h
+++ b/srchybrid/Stdafx.h
@@ -197,6 +197,11 @@
 #include <afxcoll.h>
 #include <afximpl.h>
 
+// Re-issued here on purpose: see the note with the warning block near the top
+// of this file. The MFC headers above restore the warning state, so this one
+// has to come after them to have any effect.
+#pragma warning(disable:4191) // 'type cast' : unsafe conversion from <this> to <that>
+
 #include "types.h"
 
 #ifdef _DEBUG
