diff --git a/srchybrid/BuddyButton.cpp b/srchybrid/BuddyButton.cpp
index 2183b16..c1c75f2 100644
--- a/srchybrid/BuddyButton.cpp
+++ b/srchybrid/BuddyButton.cpp
@@ -34,11 +34,13 @@ struct SBuddyData
 	HWND m_hwndButton;
 };
 
-#pragma warning(push)
-#pragma warning(disable: 5039)
-static LRESULT CALLBACK BuddyButtonSubClassedProc(HWND hWnd, UINT uMessage, WPARAM wParam, LPARAM lParam)
+// Both window procedures below are only ever entered from Win32 code, where a C++
+// exception must never arrive. C5039 asks for that to be stated in their types.
+typedef LRESULT (CALLBACK *NoThrowWndProc)(HWND, UINT, WPARAM, LPARAM) noexcept;
+
+static LRESULT CALLBACK BuddyButtonSubClassedProc(HWND hWnd, UINT uMessage, WPARAM wParam, LPARAM lParam) noexcept
 {
-	WNDPROC pfnOldWndProc = (WNDPROC)::GetProp(hWnd, s_szPropOldWndProc);
+	NoThrowWndProc pfnOldWndProc = (NoThrowWndProc)::GetProp(hWnd, s_szPropOldWndProc);
 	ASSERT(pfnOldWndProc != NULL);
 
 	SBuddyData *pBuddyData = static_cast<SBuddyData*>(::GetProp(hWnd, s_szPropBuddyData));
@@ -78,9 +80,9 @@ static LRESULT CALLBACK BuddyButtonSubClassedProc(HWND hWnd, UINT uMessage, WPAR
 
 void AddBuddyButton(HWND hwndEdit, HWND hwndButton)
 {
-	FARPROC lpfnOldWndProc = (FARPROC)::SetWindowLongPtr(hwndEdit, GWLP_WNDPROC, (LONG_PTR)BuddyButtonSubClassedProc);
-	ASSERT(lpfnOldWndProc != NULL);
-	VERIFY(::SetProp(hwndEdit, s_szPropOldWndProc, (HANDLE)lpfnOldWndProc));
+	LONG_PTR lOldWndProc = ::SetWindowLongPtr(hwndEdit, GWLP_WNDPROC, (LONG_PTR)BuddyButtonSubClassedProc);
+	ASSERT(lOldWndProc != 0);
+	VERIFY(::SetProp(hwndEdit, s_szPropOldWndProc, (HANDLE)lOldWndProc));
 
 	// Remove the 'flat' style which may have been set by 'InitWindowStyles'
 	LONG_PTR dwButtonStyle = ::GetWindowLongPtr(hwndButton, GWL_STYLE);
@@ -97,7 +99,6 @@ void AddBuddyButton(HWND hwndEdit, HWND hwndButton)
 
 	::SetWindowPos(hwndEdit, NULL, 0, 0, 0, 0, SWP_NOMOVE | SWP_NOSIZE | SWP_NOZORDER | SWP_FRAMECHANGED);
 }
-#pragma warning(pop)
 
 void DestroyIconsArr(HICON *pIcon, size_t cnt)
 {
